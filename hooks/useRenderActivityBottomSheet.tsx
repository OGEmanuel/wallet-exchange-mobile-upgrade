import { View, Text } from "react-native";
import React from "react";

const useRenderActivityBottomSheet = () => {
  return (
    <View>
      <Text>useRenderActivityBottomSheet</Text>
    </View>
  );
};

export default useRenderActivityBottomSheet;
