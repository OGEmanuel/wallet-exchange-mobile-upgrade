module.exports = {
  presets: ["babel-preset-expo"],
  plugins: [
    // Keep Reanimated plugin last per its documentation
    // 'react-native-worklets/plugin',
    // 'react-native-reanimated/plugin',
  ],
};
